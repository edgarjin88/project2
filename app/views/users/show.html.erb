<h1 align="center"> Welcome to <%= @user.username %>'s page </h1>
<div class="row">
  <div class="col-md-4 col-md-offset-4 center">
    <%# cloudinary %>
   <%= cl_image_tag(@user.image, :width => 400) if @user.image.present?%>

  </div>
</div>

<h4 align="center"><%= @user.username%>'s articles </h4>
<div class="center"> 

  <%= will_paginate @user_articles %>
  <%# why do I have to pass a argument here?  %>
  <%# user index 에서는 요구하지 않았는데? %>
</div>
<%= render 'articles/article', obj: @user_articles %>
<%# 이러면 paginate한 숫자만 포함하는거 아닌가? %>
<%# console 찍어 보자 %>

<div class="center"> 
  <%= will_paginate @user_articles %>
</div>